# Runtime server port
PORT=8787

# API route prefix
API_BASE_PATH=/api/resume

# Comma-separated origins for CORS
CORS_ORIGINS=http://localhost:4200,https://haolun-wang.pages.dev

# Local JSON source path (used in current mode)
DATA_FILE_PATH=src/data/content.i18n.json
